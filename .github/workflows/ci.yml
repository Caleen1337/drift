name: CI

on:
  push:
  workflow_dispatch:

jobs:
  core-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "5.10.1"
      - name: Run Core tests
        working-directory: Core
        run: swift test

  build-ios:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate Xcode project
        run: xcodegen generate
      - name: Build (unsigned)
        run: |
          set -o pipefail
          xcodebuild \
            -project Drift.xcodeproj \
            -scheme Drift \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build 2>&1 | tee /tmp/xcodebuild.log
      - name: Show build log on failure
        if: failure()
        run: tail -150 /tmp/xcodebuild.log
      - name: Package unsigned IPA
        if: success()
        run: |
          APP="$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name 'Drift.app' | head -n1)"
          mkdir -p Payload
          cp -R "$APP" Payload/
          zip -qr Drift-unsigned.ipa Payload
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: Drift-unsigned-ipa
          path: Drift-unsigned.ipa
